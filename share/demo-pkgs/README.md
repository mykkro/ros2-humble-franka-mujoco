# Small ROS2 demos

```bash
# Run the docker image like this for GUI support and mounted shared dir:
docker run  -v /home/myrousz/ros2-foxy-ws:/home/rosdemos/share -it --rm -e DISPLAY=${DISPLAY} -v /tmp/.X11-unix:/tmp/.X11-unix 6bca85afb25f bash
```


